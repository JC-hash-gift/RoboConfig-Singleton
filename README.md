# RoboConfig-Singleton
It is a learning file for Singleton Design Pattern. The code is of a Robot Config which consists of feature which are interrelated.


A Singleton ensures that only one instance of a class exists throughout the program.
